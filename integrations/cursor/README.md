# Cursor Integration for AEP

This integration provides Cursor rules for working with AEP projects.

## Installation

Copy the `.cursor/` directory to your project root.

## Features

- Project structure awareness
- Resource format validation
- State register guidance
- Best practices enforcement

## How It Works

Cursor reads the `.cursor/rules/aep.mdc` file and applies these rules when editing AEP projects.